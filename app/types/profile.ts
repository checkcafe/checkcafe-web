export type ProfilePlace = {
  name: string;
  slug: string;
  description?: string;
  thumbnailUrl?: string;
};

export type ProfileFavorite = {
  name: string;
  slug: string;
  streetAddress: string;
  thumbnailUrl?: string;
};
